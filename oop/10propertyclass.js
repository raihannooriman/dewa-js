class ClassName {
  constructor(parameter) {
    this.property = parameter; // property pada class dalam constructor dengan parameter "parameter"
  }
}
const variableName = new ClassName("value");
console.log(variableName);
console.log(variableName.property); // output: value karena property "property" pada class ClassName diisi dengan "value"
