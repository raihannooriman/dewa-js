const boolean = Boolean(true);
console.log(boolean); // output: true
console.log(boolean.toString()); // output: "true" string
console.log(boolean.valueOf()); // output: true, valueOf digunakan untuk mengembalikan nilai boolean yang sebenarnya (true/false) dari objek boolean yang dihasilkan oleh constructor Boolean
